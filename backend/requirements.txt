# ==============================================================================
# Backend — Python dependencies
# ==============================================================================
# Install all of these with:  pip install -r requirements.txt

# --- Web Framework ---
fastapi==0.115.5          # The API framework
uvicorn[standard]==0.32.1 # ASGI server that runs FastAPI (like Apache for Python)

# --- Configuration ---
pydantic-settings==2.6.1  # Reads .env into typed Python classes

# --- Database (Supabase) — Milestone 1 ---
supabase==2.9.1           # Supabase Python client

# --- GitHub API — Milestone 2 ---
PyGithub==2.4.0           # GitHub REST API client
PyJWT==2.9.0              # JWT generation for GitHub App auth
cryptography==43.0.3      # Required by PyJWT for RSA key signing



# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2             # Async HTTP client (pinned for supabase compatibility)

# --- Checkpointing ---
langgraph-checkpoint-postgres==2.0.14
psycopg[binary]==3.2.3
psycopg-pool==3.2.4
